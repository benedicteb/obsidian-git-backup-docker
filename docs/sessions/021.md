# Session 021 — Datadog Metrics Push

**Date**: 2026-03-16

## What was accomplished

Added a Datadog metrics push step to the existing daily `metrics.yml`
GitHub Actions workflow. The Docker Hub pull count (already fetched for
the CSV recording) is now also submitted as a gauge metric to Datadog's
EU1 region via the v1 series API.

### Changes made

1. **`.github/workflows/metrics.yml`** — Added a "Push metric to
   Datadog" step between the fetch/record step and the commit/push
   step. The step:
   - Skips gracefully when `DD_API_KEY` is not configured
   - Uses `printf %d` for JSON construction (injection-safe)
   - Handles network failures (HTTP 000/empty) without crashing
   - Provides actionable error messages for HTTP 403 specifically
   - Never fails the workflow — all errors produce warnings only

2. **`docs/adr/0020-datadog-metrics-push.md`** — Architecture decision
   record documenting the Datadog integration, including the choice to
   use the existing workflow rather than a new one, EU1 hardcoding
   rationale, and security considerations.

## Decisions made

- **Datadog region**: EU1 hardcoded (user's explicit choice). No
  `DD_SITE` variable — a deliberate simplicity trade-off for a
  single-maintainer project.
- **Metric name**: `obsidian_git_backup.docker_pulls` — namespaced
  under the project name.
- **Metric type**: `gauge` — appropriate for a cumulative counter
  where we report the absolute value.
- **Optional behavior**: The step is skipped if `DD_API_KEY` is not
  set. Forks and contributors are unaffected.

## Subagent reviews

### Linux expert
- Recommended `printf %d` for JSON construction instead of shell
  string-break pattern — **addressed**.
- Identified curl network failure could produce empty `HTTP_CODE`
  causing arithmetic error under `set -e` — **addressed** with
  `|| true` and case-based handling.
- Noted API key is safe from log exposure due to GitHub's automatic
  secret masking — accepted as sufficient.

### UX designer
- Suggested completing tag values in header comment — **addressed**.
- Recommended actionable HTTP 403 error message — **addressed**.
- Noted EU1 hardcoding could confuse other users — accepted as
  intentional for this project (single maintainer on EU1).
- Suggested `DD_SITE` configurability — deferred (unnecessary
  complexity for current use case).

### Obsidian expert
- No concerns — change is entirely in CI, doesn't touch vault, sync,
  or plugin behavior.
- Noted ADR-0018 staleness regarding "no external credentials" —
  **addressed** in ADR-0020 which amends ADR-0018.

## Setup required

To enable the Datadog push, add a GitHub secret:
- **Name**: `DD_API_KEY`
- **Value**: A Datadog API key from Organization Settings > API Keys
  (EU1 console). Must have metrics ingestion scope.

## Open questions

None.
