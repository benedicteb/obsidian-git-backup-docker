# =============================================================================
# obsidian-headless Update Checker
#
# Runs daily at 08:00 UTC. Checks the npm registry for a newer version of
# obsidian-headless than what is pinned in the Dockerfile. If a new version
# exists, opens a PR with:
#   - The Dockerfile version bump
#   - Changelog entries from upstream (obsidianmd/obsidian-headless)
#   - Dependency diff (added/removed/changed npm dependencies)
#   - Links to the upstream commit comparison
#
# The PR is not auto-merged — the maintainer reviews the changes and decides
# whether to merge. This is intentional: obsidian-headless is pre-stable
# (0.x) and breaking changes can land in any release.
#
# If an update PR already exists (same branch name), the workflow skips
# creating a duplicate.
#
# No secrets required beyond the default GITHUB_TOKEN.
# =============================================================================

name: Check obsidian-headless Update

on:
  schedule:
    # Daily at 08:00 UTC
    - cron: '0 8 * * *'

  # Allow manual trigger for testing
  workflow_dispatch:

# Only one update check at a time
concurrency:
  group: check-headless-update
  cancel-in-progress: false

jobs:
  check-update:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write       # Create branch and push commits
      pull-requests: write  # Open PRs

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ---------------------------------------------------------------
      # Extract the currently pinned version from the Dockerfile
      # ---------------------------------------------------------------
      - name: Get current version
        id: current
        run: |
          set -euo pipefail

          # Match: npm install -g obsidian-headless@X.Y.Z
          CURRENT=$(grep -oP 'obsidian-headless@\K[0-9]+\.[0-9]+\.[0-9]+' Dockerfile)

          if [ -z "$CURRENT" ]; then
            echo "::error::Could not extract obsidian-headless version from Dockerfile"
            exit 1
          fi

          echo "version=${CURRENT}" >> "$GITHUB_OUTPUT"
          echo "Current pinned version: ${CURRENT}"

      # ---------------------------------------------------------------
      # Query npm registry for the latest published version
      # ---------------------------------------------------------------
      - name: Get latest version from npm
        id: latest
        run: |
          set -euo pipefail

          REGISTRY_URL="https://registry.npmjs.org/obsidian-headless"
          RESPONSE=$(curl -fsSL "$REGISTRY_URL")

          LATEST=$(printf '%s' "$RESPONSE" | jq -r '.["dist-tags"].latest')

          if [ -z "$LATEST" ] || [ "$LATEST" = "null" ]; then
            echo "::error::Could not determine latest version from npm registry"
            exit 1
          fi

          echo "version=${LATEST}" >> "$GITHUB_OUTPUT"
          echo "Latest npm version: ${LATEST}"

          # Also extract gitHead SHAs for commit comparison links
          CURRENT="${{ steps.current.outputs.version }}"

          CURRENT_SHA=$(printf '%s' "$RESPONSE" | jq -r --arg v "$CURRENT" '.versions[$v].gitHead // empty')
          LATEST_SHA=$(printf '%s' "$RESPONSE" | jq -r --arg v "$LATEST" '.versions[$v].gitHead // empty')

          echo "current_sha=${CURRENT_SHA}" >> "$GITHUB_OUTPUT"
          echo "latest_sha=${LATEST_SHA}" >> "$GITHUB_OUTPUT"

          # Extract dependency lists for both versions (as JSON objects)
          CURRENT_DEPS=$(printf '%s' "$RESPONSE" | jq -c --arg v "$CURRENT" '.versions[$v].dependencies // {}')
          LATEST_DEPS=$(printf '%s' "$RESPONSE" | jq -c --arg v "$LATEST" '.versions[$v].dependencies // {}')

          echo "current_deps=${CURRENT_DEPS}" >> "$GITHUB_OUTPUT"
          echo "latest_deps=${LATEST_DEPS}" >> "$GITHUB_OUTPUT"

      # ---------------------------------------------------------------
      # Compare versions — exit early if already up to date
      # ---------------------------------------------------------------
      - name: Check if update needed
        id: check
        run: |
          set -euo pipefail

          CURRENT="${{ steps.current.outputs.version }}"
          LATEST="${{ steps.latest.outputs.version }}"

          if [ "$CURRENT" = "$LATEST" ]; then
            echo "Already up to date (${CURRENT}). Nothing to do."
            echo "needed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "Update available: ${CURRENT} → ${LATEST}"
          echo "needed=true" >> "$GITHUB_OUTPUT"

      # ---------------------------------------------------------------
      # Check if an update PR already exists for this version
      # ---------------------------------------------------------------
      - name: Check for existing PR
        if: steps.check.outputs.needed == 'true'
        id: existing_pr
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          LATEST="${{ steps.latest.outputs.version }}"
          BRANCH="deps/obsidian-headless-${LATEST}"

          # Check if branch already exists on remote
          if git ls-remote --heads origin "$BRANCH" | grep -q "$BRANCH"; then
            echo "Branch ${BRANCH} already exists. Checking for open PR..."
            OPEN_PR=$(gh pr list --head "$BRANCH" --state open --json number --jq '.[0].number // empty')
            if [ -n "$OPEN_PR" ]; then
              echo "PR #${OPEN_PR} already open for this version. Skipping."
              echo "exists=true" >> "$GITHUB_OUTPUT"
              exit 0
            fi
          fi

          echo "exists=false" >> "$GITHUB_OUTPUT"

      # ---------------------------------------------------------------
      # Fetch the upstream CHANGELOG.md and extract relevant entries
      # ---------------------------------------------------------------
      - name: Fetch upstream changelog
        if: steps.check.outputs.needed == 'true' && steps.existing_pr.outputs.exists == 'false'
        id: changelog
        run: |
          set -euo pipefail

          CURRENT="${{ steps.current.outputs.version }}"
          LATEST="${{ steps.latest.outputs.version }}"

          # Fetch CHANGELOG.md from obsidianmd/obsidian-headless (master branch)
          CHANGELOG=$(curl -fsSL \
            "https://raw.githubusercontent.com/obsidianmd/obsidian-headless/master/CHANGELOG.md" \
            2>/dev/null || true)

          if [ -z "$CHANGELOG" ]; then
            echo "Could not fetch upstream CHANGELOG.md — will use commit log instead."
            echo "entries=_Could not fetch upstream CHANGELOG.md._" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Extract all version sections between current (exclusive) and latest (inclusive).
          # The CHANGELOG uses "## X.Y.Z" headers.
          #
          # Strategy: use awk to collect lines from versions > CURRENT up to and
          # including LATEST. We compare version strings lexicographically, which
          # works for 0.0.x semver where all components are single digits.
          # For robustness, we include all sections between the two versions.
          ENTRIES=$(printf '%s\n' "$CHANGELOG" | awk -v current="$CURRENT" -v latest="$LATEST" '
            /^## [0-9]+\.[0-9]+\.[0-9]+/ {
              ver = $2
              # We want versions > current AND <= latest
              in_range = (ver != current && ver != "")
              # Stop if we hit the current version or older
              if (ver == current) { in_range = 0 }
            }
            in_range { print }
          ')

          if [ -z "$ENTRIES" ]; then
            ENTRIES="_No changelog entries found between ${CURRENT} and ${LATEST}._"
          fi

          # Write to a file to avoid GITHUB_OUTPUT escaping issues with multiline
          printf '%s\n' "$ENTRIES" > /tmp/changelog_entries.txt
          echo "file=/tmp/changelog_entries.txt" >> "$GITHUB_OUTPUT"

      # ---------------------------------------------------------------
      # Build the dependency diff section
      # ---------------------------------------------------------------
      - name: Build dependency diff
        if: steps.check.outputs.needed == 'true' && steps.existing_pr.outputs.exists == 'false'
        id: depdiff
        run: |
          set -euo pipefail

          CURRENT_DEPS='${{ steps.latest.outputs.current_deps }}'
          LATEST_DEPS='${{ steps.latest.outputs.latest_deps }}'

          DIFF=$(jq -r -n \
            --argjson current "$CURRENT_DEPS" \
            --argjson latest "$LATEST_DEPS" '
            # Find all unique keys
            ([$current | keys[], $latest | keys[]] | unique) as $all_keys |
            [$all_keys[] |
              . as $key |
              ($current[$key] // null) as $old |
              ($latest[$key] // null) as $new |
              if $old == $new then empty
              elif $old == null then "- **Added**: `\($key)` \($new)"
              elif $new == null then "- **Removed**: `\($key)` ~~\($old)~~"
              else "- **Changed**: `\($key)` \($old) → \($new)"
              end
            ] | if length == 0 then "_No dependency changes._" else join("\n") end
          ')

          printf '%s\n' "$DIFF" > /tmp/depdiff.txt
          echo "file=/tmp/depdiff.txt" >> "$GITHUB_OUTPUT"

      # ---------------------------------------------------------------
      # Update the Dockerfile and create the PR
      # ---------------------------------------------------------------
      - name: Create update PR
        if: steps.check.outputs.needed == 'true' && steps.existing_pr.outputs.exists == 'false'
        env:
          GH_TOKEN: ${{ github.token }}
          CURRENT: ${{ steps.current.outputs.version }}
          LATEST: ${{ steps.latest.outputs.version }}
          CURRENT_SHA: ${{ steps.latest.outputs.current_sha }}
          LATEST_SHA: ${{ steps.latest.outputs.latest_sha }}
        run: |
          set -euo pipefail

          BRANCH="deps/obsidian-headless-${LATEST}"

          # Configure git identity
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Create branch
          git checkout -b "$BRANCH"

          # Update the version in the Dockerfile
          sed -i "s/obsidian-headless@${CURRENT}/obsidian-headless@${LATEST}/" Dockerfile

          # Verify the change was made
          if ! grep -q "obsidian-headless@${LATEST}" Dockerfile; then
            echo "::error::Failed to update version in Dockerfile"
            exit 1
          fi

          # Commit and push
          git add Dockerfile
          git commit -m "deps: bump obsidian-headless ${CURRENT} → ${LATEST}"
          git push origin "$BRANCH"

          # Build the PR body file
          CHANGELOG_ENTRIES=$(cat /tmp/changelog_entries.txt 2>/dev/null || echo "_Could not fetch changelog._")
          DEP_DIFF=$(cat /tmp/depdiff.txt 2>/dev/null || echo "_Could not compute dependency diff._")

          # Build commit comparison link if both SHAs are available
          COMMIT_LINK=""
          if [ -n "$CURRENT_SHA" ] && [ -n "$LATEST_SHA" ]; then
            COMMIT_LINK="- [View all commits (${CURRENT_SHA:0:7}...${LATEST_SHA:0:7})](https://github.com/obsidianmd/obsidian-headless/compare/${CURRENT_SHA}...${LATEST_SHA})"
          fi

          PR_BODY="/tmp/pr_body.md"
          {
            echo "## obsidian-headless ${CURRENT} → ${LATEST}"
            echo ""
            echo "A new version of [obsidian-headless](https://github.com/obsidianmd/obsidian-headless) has been published to npm."
            echo ""
            echo "> **This PR does not auto-merge.** obsidian-headless is pre-stable (0.x) — please review the changes below before merging."
            echo ""
            echo "### Upstream changelog"
            echo ""
            echo "$CHANGELOG_ENTRIES"
            echo ""
            echo "### Dependency changes"
            echo ""
            echo "$DEP_DIFF"
            echo ""
            echo "### Links"
            echo ""
            echo "- [npm: obsidian-headless@${LATEST}](https://www.npmjs.com/package/obsidian-headless/v/${LATEST})"
            if [ -n "$COMMIT_LINK" ]; then
              echo "$COMMIT_LINK"
            fi
            echo "- [Upstream repo](https://github.com/obsidianmd/obsidian-headless)"
            echo ""
            echo "### What to check before merging"
            echo ""
            echo "- [ ] Read the changelog entries above for breaking changes"
            echo "- [ ] Check if any dependency changes affect our build (e.g., native modules)"
            echo "- [ ] Consider testing the new version locally: \`docker build --build-arg TARGETARCH=amd64 -t test .\`"
          } > "$PR_BODY"

          # Create the PR
          gh pr create \
            --base main \
            --head "$BRANCH" \
            --title "deps: bump obsidian-headless ${CURRENT} → ${LATEST}" \
            --body-file "$PR_BODY"

          echo "PR created successfully."
